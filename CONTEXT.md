# ZCode 领域词汇表（Domain Glossary）

本仓库的领域词汇表。分两部分：插件商店（Plugin Store）术语，以及远程设备投射（Remote Device Projection）术语。

## Language

### 远程设备投射（Remote Device Projection）

**Projected Session（投射会话）**:
物理上只存在于被连设备（B）上的会话。连接端（A）只显示与操作它，不持有它的数据或副本。
一次对话自始至终是同一个会话，无论从哪一端打开。
_Avoid_: "远程会话"（易被误读为 A 端另有一份）、"同步过来的会话"

**Projecting Device（投射端，A）**:
发起连接、显示被连设备项目与会话的一端。不持久化会话数据。
_Avoid_: 控制端、主端（隐含主从，但两端是对等的）

**Projected Device（被投射端，B）**:
承载项目与会话数据、供对端显示与操作的一端。其上的会话独立于是否有连接存在。
_Avoid_: 从端、被控端（暗示被动，但 B 本地可独立操作）

**Session Transcript Loading（会话内容加载）**:
投射端打开会话时按需加载：先取最近内容即可交互，向上滚动再取更早历史。
依据：单个会话可能有数百条消息与数千片段，一次性全量拉取既慢又占内存。

**Session Continuation（会话续接）**:
投射端可直接在提交框继续被投射设备的会话，执行发生在被投射设备。
两端对同一会话的续接能力对等。
_Avoid_: 只读模式（本项目不存在"仅查看"的会话视图）

**Device Overview（设备概览）**:
连上被投射设备后的主区呈现：该设备已添加的项目列表、各项目会话数、以及「添加项目」入口。
从概览进入某个项目才进入工作区。设备概览是"连设备"语义的落点，取代旧流程的"选目录"。

**Credential Handling（凭据处理）**:
连接入口引用系统密钥文件路径（如 `~/.ssh/id_ed25519_imac`），密钥本体不进投射端数据库；
密码方式走凭据服务加密存储。

**Projected Project List（投影项目清单）**:
被投射设备的项目清单取「已登记项目」与「实际有会话的项目」的并集（去重）——
新登记但尚无会话的项目必须可见，否则用户添加后看不到会困惑。

**Local Continuation Independence（本机续接独立性）**:
被投射设备上的会话操作不依赖投射端是否连接。投射端断开只意味着"投射端看不到"，
对被投射设备上的会话、执行与继续操作毫无影响，无需任何额外机制。

**Single Device Scope（单设备范围）**:
首版只支持同时连接一台被投射设备；数据结构按列表存（将来增设备不需重构），
但 UI 只暴露单台。

**Disconnected Retention（断开后留存）**:
投射端断开后只保留设备条目（连接入口），不保留项目清单——项目清单属于被投射设备的数据，
连接建立后重新获取。
_Avoid_: 缓存项目清单、离线项目列表

**Archived Session Exclusion（归档会话排除）**:
投射的会话列表不显示已归档会话。

**Connection Entry（连接入口）**:
添加与管理被投射设备的唯一入口位于**设置页**「远程设备」。侧边栏不提供连接入口。
_Avoid_: 侧边栏 + 号连设备、项目区连设备

**Session Origin（会话归属）**:
在投射项目里新建的会话，归属被投射设备，其数据与执行都在被投射设备上。
投射端不产生"本地副本"——从哪一端发起的对话，都是同一个会话。
_Avoid_: 远程会话（暗示归属在发起端）、镜像会话

**Device Boundary（设备边界）**:
被投射设备只提供项目与会话；一切"如何显示、如何组织"的设置属于投射端的本地事务，
存在投射端。投射端不向被投射设备写入偏好。
_Avoid_: 双向设置同步（会模糊两端职责，也违背"隐藏不影响 B"）

**Visibility Preference（显示偏好）**:
投射端勾选显示哪些项目的选择，**只作用于投射端**。被投射设备的项目列表不受任何影响——
在 A 上隐藏某项目，B 本机仍照常可见可操作。
_Avoid_: 项目开关（易误读为改对端项目列表）

**Projection Scope（投射范围）**:
投射端显示哪些被投射设备的项目，由投射端用户在设置页勾选决定；候选项来自被投射设备
**已添加的项目**（其 `recentProjects`）。缺省不显示——连上设备只是建立能力，显示什么由用户选。

**Remote Project Addition（远程添加项目）**:
在投射端浏览**被投射设备的文件系统**选择目录，把该项目加入被投射设备的项目列表。
项目归属与数据始终在被投射设备。
_Avoid_: 新建项目（不创建目录，只是把既有目录登记为项目）

**Disconnected Projection（断开态投射）**:
连接不存在时投射端对某设备项目的呈现：条目**灰显保留**在项目列表中，只含连接入口，
不含会话数据；用户点击即重连，无需重填连接信息。
_Avoid_: 离线缓存（暗示存了数据）、历史记录（暗示只是一条日志）

**Retained State（留存物，投射端）**:
投射端唯一持久化的信息：**连接入口**（主机、认证、远端目录）。会话数据与会话索引一律不留存。
_Avoid_: 缓存、副本、离线数据

**Projection（投射）**:
A 端显示 B 端数据的行为。区别于 Sync（同步）：投射不产生副本，断开即消失。
_Avoid_: 同步、镜像、复制

### 工单索引

远程设备功能的实施工单位于 `.scratch/remote-device/issues/`，按依赖顺序编号：

| # | 工单 | 阻塞于 |
|---|---|---|
| 01 | 会话续接真正执行（最高风险，判决点） | — |
| 02 | 不选目录直接连设备 | — |
| 03 | 设备概览主区 | 02 |
| 04 | 侧边栏投射与项目勾选 | 02 |
| 05 | 浏览被投射设备文件系统添加项目 | 03 |
| 06 | 会话内容按需加载 | 01 |
| 07 | 收敛与清理 | 01–06 |

规格说明：`.agents/plans/spec-remote-device.md`

## 插件商店（Plugin Store）

### 市场与来源

**Official Marketplace（官方市场）**:
ZCode 官方运营的唯一分发渠道，市场 id 为 `zcode-plugins-official`，内容 = 内置插件 + CDN 插件。是"分发渠道"而非"作者归属"——其中可以收录社区作者的插件。
_Avoid_: "官方"泛指一切受信市场

**Builtin Plugin（内置插件）**:
随应用包一起分发、启动时播种进官方市场的插件。是官方插件的子集。
_Avoid_: 预装插件、bundled plugin（口语可用，文档统一"内置"）

**CDN Plugin（CDN 插件）**:
官方市场中通过官方 CDN 以 sha256 校验的 zip 包分发、按需下载安装的插件。
_Avoid_: 网络插件、在线插件

**Personal Source（个人来源）**:
用户自行添加的一切插件来源：git/GitHub/URL/本地目录市场、inline 插件。
_Avoid_: 无

**Catalog Auto-Refresh（目录自动刷新）**:
进入商店页时对 Official Marketplace 目录的节流后台刷新，用户无感知；只覆盖官方市场。
_Avoid_: 与 Manual Refresh 混用；把它称作"检查更新"（更新角标只是刷新的副产物）

**Manual Refresh（手动刷新）**:
商店页顶栏刷新按钮触发的全市场刷新，不受自动刷新节流影响。
_Avoid_: 刷新、检查更新（口语可用，文档统一"手动刷新"）

### 商店页结构

**Public Segment（公开）**:
商店列表页的分段之一，展示且仅展示官方市场的目录（Featured + 分类区块）。
_Avoid_: 官方 tab、商店 tab

**Personal Segment（个人）**:
商店列表页的另一分段，展示全部个人来源的目录，按市场分组。
_Avoid_: 第三方 tab、我的 tab

**Featured（精选）**:
公开分段顶部的策展区，名单由官方 CDN 目录的 `featured` 字段远程控制。仅存在于公开分段。
_Avoid_: 与 Recommended 混用

**Installed Strip（已安装条）**:
列表页顶部的一排已安装插件图标，点击图标进入详情页。
_Avoid_: 已安装列表（那是 Manage Installed 视图的事）

**Manage Installed View（管理已安装视图）**:
已安装条右侧齿轮进入的管理界面，承载插件级启停开关、更新、卸载、启用状态筛选。
_Avoid_: Installed tab（旧 IA 术语，已废弃）

### 元数据

**Store Listing（商店信息）**:
目录条目携带的展示性元数据：显示名、icon、分类、开发者、网站/隐私政策/服务条款链接、hero 图、示例提示词。描述"如何在商店里呈现"，不影响插件功能。
_Avoid_: 插件元数据（含糊，可能指 manifest）

**Plugin Manifest（插件清单）**:
插件包内 `plugin.json` 的功能性定义（commands/agents/skills/hooks/mcpServers/userConfig…）。描述"插件是什么、做什么"。
_Avoid_: marketplace.json（那是目录，不是清单）

**Example Prompt（示例提示词）**:
Store Listing 提供的可点击提示词，点击后新建会话并预填（不自动发送）。是详情页唯一的"新建会话"入口。
_Avoid_: 快捷指令、prompt 模板、立即试用

### 生命周期状态

**Plugin Lifecycle（插件生命周期）**:
用户从发现插件开始，经过查看、安装、配置、启停、使用、检查更新、升级、持久化恢复，直到卸载或恢复内置插件的完整产品路径。每个阶段都必须同时验证可见 UI 状态和对应的持久化或运行时结果。
_Avoid_: 仅把“安装成功”称为完整生命周期

**Restorable Builtin（可恢复内置插件）**:
被用户卸载并进入持久化抑制状态的 Builtin Plugin。应用重启不得自动重新播种；它继续出现在 Public Segment，并通过“安装”入口执行干净恢复。
_Avoid_: 未安装 CDN 插件、临时禁用的内置插件

**Orphaned Installed Plugin（孤立已安装插件）**:
对应 Personal Source 已被删除、但安装目录和用户数据仍保留的插件。它仍可使用、配置、启停和卸载；来源重新添加前不能更新，重新添加同一来源后恢复目录关联。
_Avoid_: 安装损坏、manifest 缺失、已卸载插件
